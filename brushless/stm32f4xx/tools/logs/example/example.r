post_process <- function(data, N, frequence, t1, t2) {
#  library('pracma')
#  data$raw_angle <- (data$raw_angle - 0.4191602) %% (2*pi)
#  data$anglep <- -data$angle %% (2*pi) 
#  data$diff <-  data$anglep - data$raw_angle

  return(data)
}
